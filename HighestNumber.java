 


//Find the highest number on ArrayList

public class HighestNumber {
 
  public static void main() {
    ArrayList l = new ArrayList(); 
    int max = 0; //assumes + numbers 
    int temp; 
         for(ArrayList l : veic.getVeiculos()) { 
             //temp = Convert.ToInt32(s); 
             if(max < temp) {
                max = temp;
             } 
         } 
    return max;
   }
} 
